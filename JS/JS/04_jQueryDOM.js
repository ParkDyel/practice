$(document).ready(function () {

});

function toggleClassPushed() {
  //if defined class, remove, but not define class set arg
  // $(selected).addClass(arg)
  // $(selected).removeClass(arg)
  $('#h1 h1').toggleClass('item')
}

// attr 메소드
// $(selector).attr(attributeName);
// $(selector).attr(attributeName, value);
// $(selector).removeAttr(attributeName);


